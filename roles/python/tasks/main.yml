---

- name: Install reuired system packages
  become: true
  apt:
    name: "{{ item }}"
  with_items:
    - python-pip
    - python-psycopg2
    - python3-distutils
  tags:
    - python

- name: Install PIP packages
  become: true
  pip:
    name: virtualenv
  tags:
    - python