---

- name: Install reuired system packages
  become: true
  apt:
    name: "{{ item }}"
  with_items:
    - python-pip
    - python-psycopg2
  tags:
    - python

- name: Install PIP packages
  become: true
  pip:
    name: virtualenv
  tags:
    - python